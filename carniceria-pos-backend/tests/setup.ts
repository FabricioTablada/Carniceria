/**
 * tests/setup.ts
 * -----------------------------------------------------------------------------
 * Configuracion global para las pruebas. Se ejecuta antes de cada suite.
 */
process.env.NODE_ENV = 'test';
